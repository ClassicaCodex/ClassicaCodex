<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <Version>3.6.15</Version>
    <AssemblyVersion>3.6.15.0</AssemblyVersion>
    <FileVersion>3.6.15.0</FileVersion>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>ClassicaCodex.Core</RootNamespace>
    <!-- Enables the compiler's structural checks on /// doc comments -
         specifically CS1587 ("XML comment is not placed on a valid language
         element"), which is exactly what would have caught the nine
         orphaned/misattached doc-comment blocks found in the first review
         pass of this codebase, automatically, on every future build.
         CS1591 ("missing XML comment for publicly visible member") is
         suppressed: this project documents public members selectively, not
         universally, so demanding one on every public method would flood
         the build with thousands of warnings unrelated to the actual
         problem this is here to catch.
         CS1573 ("parameter has no matching param tag, but other parameters
         do") is suppressed for the same reason one level down: a comment
         here explains the parameters that need explaining and leaves the
         obvious ones alone. Satisfying it would mean a tag for every
         cancellationToken in the codebase - noise that would bury the
         structural warnings above, which are the ones worth reading. -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <NoWarn>$(NoWarn);CS1591;CS1573</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!-- The Fictional Ancient Reactions content. Embedded rather than copied
         beside the executable, so a debate cannot go missing from a download
         the way the Icons folder can, and rather than seeded into SQLite, for
         the reasons ReactionLibrary gives at length: this is content that
         ships with the program, not material the reader owns.

         ReactionLibrary finds these by looking for ".Reactions.Packs." in the
         manifest name, which is the logical name this Include produces
         (ClassicaCodex.Core.Reactions.Packs.clouds-423.json). A pack added to
         the folder is picked up with no further wiring - and if one is ever
         moved out of this folder, it simply stops being found, which the
         shipped-content test turns into a failed build rather than a feature
         that quietly has less in it. -->
    <EmbeddedResource Include="Reactions\Packs\*.json" />
  </ItemGroup>

  <ItemGroup>
    <!-- Reconcile() is the fix for Create Translation silently saving an
         empty edition. It's an internal detail of how a model's reply is
         matched back to real passages, not public API - but it's exactly the
         kind of logic that fails quietly, so the tests can see it.

         Named for the single test project this solution actually has. There
         was briefly a second one for Core, which is why these tests still
         declare a ClassicaCodex.Core.Tests namespace - harmless, since a
         namespace need not match its assembly, and the grouping it gives in
         Test Explorer is worth keeping. -->
    <InternalsVisibleTo Include="ClassicaCodex.Data.Tests" />
  </ItemGroup>

</Project>
